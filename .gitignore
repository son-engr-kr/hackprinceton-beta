# Root-level
/plan*.md
/graph-viz-plan.md
/MIGRATION.md
/.env
/.env.*
!/.env.example

# Internal / non-public (Korean drafts, private planning, Claude memory)
/memory/
/brainstorming/
/ande/
/knot_api_data/
/db.md

# OS
.DS_Store
Thumbs.db

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log

# PDFs (hackathon packets etc — too large / not source)
*.pdf

# ---- web/ (Next.js frontend — Vercel) ----
web/node_modules/
web/.next/
web/out/
web/build/
web/coverage/
web/.vercel/
web/*.tsbuildinfo
web/next-env.d.ts
web/.env*
web/*.pem

# ---- api/ (FastAPI backend — Cloud Run) ----
# .env lives at repo root (shared between Python config.py and Node spectrum_loop.mjs)
api/.venv/
api/__pycache__/
api/**/__pycache__/
api/webhooks.jsonl
api/adherence.jsonl
api/analyzed_*.json
api/*.log
# iMessage photo cache (receipts / food pics forwarded from Messages.app)
api/assets/incoming/
# Node orchestrator deps (spectrum-ts, @photon-ai/imessage-kit) — installed locally
api/imessage/node_modules/
# Google OAuth client secret (download per environment, never commit)
api/gcal_client.json

# ---- image-gen/ (SDXL pipeline) ----
image-gen/.venv/
image-gen/venv/
image-gen/env/
**/__pycache__/
*.py[cod]
*.so
*.egg-info/
image-gen/.python-version
image-gen/build/
image-gen/dist/
image-gen/.pytest_cache/
image-gen/.coverage
image-gen/htmlcov/
image-gen/.env
image-gen/.env.*
!image-gen/.env.example

# Generated images are large; uncomment to keep them out of git
# image-gen/images/
# image-gen/images-old/

# Secrets (defense in depth)
*.pem
*.key
id_rsa*
